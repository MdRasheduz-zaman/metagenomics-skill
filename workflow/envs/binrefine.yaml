# Isolated conda env for multi-binner refinement: MaxBin2 + CONCOCT + DAS_Tool consensus.
# Created by `--use-conda`.
name: metagx-binrefine
channels: [conda-forge, bioconda]
dependencies:
  - maxbin2 >=2.2.7
  - concoct >=1.1
  - das_tool >=1.1.6
  - samtools >=1.18
