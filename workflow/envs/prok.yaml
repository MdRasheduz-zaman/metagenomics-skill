# Isolated conda env for prokaryotic bin taxonomy/quality (gtdbtk + checkm2 conflict with
# other tools, so they get their own env). Created by `--use-conda`.
name: metagx-prok
channels: [conda-forge, bioconda]
dependencies:
  - gtdbtk >=2.4
  - checkm2 >=1.0
